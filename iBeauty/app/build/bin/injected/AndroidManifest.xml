<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.jueze.ibeauty"
	android:versionCode="1"
	android:versionName="1.0">

	<uses-sdk
		android:minSdkVersion="16"
		android:targetSdkVersion="27"/>

	<uses-permission
		android:name="android.permission.INTERNET"/>

	<uses-permission
		android:name="android.permission.ACCESS_NETWORK_STATE"/>

	<uses-permission
		android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

	<uses-permission
		android:name="android.permission.READ_EXTERNAL_STORAGE"/>

	<application
		android:allowBackup="true"
		android:icon="@mipmap/icon"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:label="@string/app_name"
		android:name=".MyApplication"
		android:theme="@style/AppTheme"
		android:resizeableActivity="false">

		<activity
			android:name=".MainActivity"
			android:label="@string/app_name">

			<intent-filter>

				<action
					android:name="android.intent.action.MAIN"/>

				<category
					android:name="android.intent.category.LAUNCHER"/>

			</intent-filter>

		</activity>

		<activity
			android:name=".BasicManualActivity"/>

		<activity
			android:name=".BasicManualNoteActivity"/>

		<activity
			android:name=".MdBlendentActivity"/>

		<activity
			android:name=".Base64Activity"/>

		<activity
			android:name=".IappSourceManagerActivity"/>

		<activity
			android:name=".RainyunActivity"/>

		<activity
			android:name=".RainyunAppActivity"/>

		<activity
			android:name=".AppManagerActivity"/>

		<activity
			android:name=".WebSourceActivity"/>

		<activity
			android:name=".MdIconActivity"/>

		<activity
			android:name=".BookMarkActivity"/>

		<activity
			android:name=".OpenSourceActivity"/>

		<activity
			android:name=".BrowserActivity"/>

		<activity
			android:name=".ImageToolActivity"/>

		<activity
			android:name="com.linchaolong.android.imagepicker.cropper.CropImageActivity"
			android:theme="@style/Base.Theme.AppCompat"/>

		<activity
			android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
			android:theme="@style/Base.Theme.AppCompat"/>

		<meta-data
			android:name="android.max_aspect"
			android:value="4.0"/>

	</application>

</manifest>

